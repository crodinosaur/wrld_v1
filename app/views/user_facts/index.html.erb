<h1>All users</h1>

<%= will_paginate @users %>

<ul>
  <% @users.each do |user| %>
    <li>
    	<%= gravatar_for user %>
    	<%= link_to user.full_name, user_fact_path(user.id) %>
    	<%= user.country %>
    	<%= user.dob %>
    </li><br>
  <% end %>
</ul>
