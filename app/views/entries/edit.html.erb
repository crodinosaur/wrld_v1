<%- model_class = Entry -%>
<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.edit', 'Edit %{model}'], :model => Entry %></h1>
</div>

Submitted score: <%= @entry.score %><br>
<br>
New score:<br>
<%= form_for [@event, @entry] do |f| %>
	<%= f.text_field :score, placeholder: "score" %>
	<%= f.submit "Submit" %>
<% end %>